add hook description in hook function comment
+ config values (example at least)
+ order tabs by name
+ group hooks by display/action an alphabetical order
+ _postProcess function + basic admin form flow
+ try/catch in hooks (and rest ?)
+ include prestashop compat options
	+ warning (string)
	+ limited_countries = array();
+ use display() in display hooks
+ overide functions()

+ collect stats on used hooks

+ generate a zip with dir, file and logo.png
+ generate templates
+ generate translation files ?