<?php
/*
* {{ name }}
*
* @author {{ author }}
* Generated by PSModuleBuilder by seb7 http://seb7.com
*/

if (!defined('_PS_VERSION_'))
	exit;

class {{ name }} extends Module
{
 	function __construct()
 	{
 	 	$this->name = '{{ name }}';
 	 	$this->tab = '{{ tab }}';
		$this->need_instance = {{ need_instance }};

		parent::__construct();

		$this->displayName = $this->l('{{ displayName }}');
		$this->description = $this->l('{{ description }}');

		$this->version = '{{ version }}';
		$this->author = '{{ author }}';
 	}

 	/**
 	* @return bool success
 	**/
 	function install()
 	{
 		return parent::install();
 	}

    /**
    * @return bool success
    **/
    function uninstall()
    {
        return parent::uninstall();
    }

        /**
     * Activate current module.
     *
     * @param bool $forceAll If true, enable module for all shop
     */
    public function enable($forceAll = false)
    {
        return parent::enable($forceAll);
    }

        /**
     * Desactivate current module.
     *
     * @param bool $forceAll If true, disable module for all shop
     */
    public function disable($forceAll = false)
    {
        return parent::disable($forceAll);
    }

{% for element_key, element_value in form if element_value == 1 %}
    public function hook{{ element_key|ucfirst }}($params)
    {

    }
{% endfor %}
}