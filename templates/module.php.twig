<?php
/*
* {{ name }}
*
* @author {{ author }}
* @documentation http://doc.prestashop.com/display/PS15/Creating+a+PrestaShop+module
* Generated by PSModuleBuilder by seb7 http://seb7.com
*/

if (!defined('_PS_VERSION_'))
	exit;

class {{ name }} extends Module
{
    public function __construct()
    {
        $this->name = '{{ name|lower }}';
        $this->tab = '{{ tab }}';
        $this->need_instance = {% if need_instance == false %}0{% else %}1{% endif %};

        parent::__construct();

        $this->displayName = $this->l('{{ displayName }}');
        $this->description = $this->l('{{ description }}');

        $this->version = '{{ version }}';
        $this->author = '{{ author }}';
    }

    /**
    * @return bool success
    **/
    public function install()
    {
        return parent::install() 
            && $this->dbInstall() 
{% for element_key, element_value in form if element_value == 1 %}
            && $this->registerHook('{{ element_key }}')
{% endfor %}            ;
 	}

    /**
    * @return bool success
    **/
    public function uninstall()
    {
        return parent::uninstall() && $this->dbUninstall();
    }

{% if( has_config is defined and has_config == true) %}
    /**
    * Module admin page
    **/
    public function getContent()
    {
        echo '<p>Here is the module configuration.</p>';
    }
{% endif %}
    /**
    * Activate current module.
    *
    * @param bool $forceAll If true, enable module for all shop
    */
    public function enable($forceAll = false)
    {
        return parent::enable($forceAll);
    }

    /**
    * Desactivate current module.
    *
    * @param bool $forceAll If true, disable module for all shop
    */
    public function disable($forceAll = false)
    {
        return parent::disable($forceAll);
    }

{% for element_key, element_value in form if element_value == 1 %}
    /**
    * hook{{ element_key|ucfirst }}
    *
    * 
    **/
    public function hook{{ element_key|ucfirst }}($params)
    {

    }

{% endfor %}

    /**
    * Module tables installation
    * 
    * @return bool success
    **/
    protected function dbInstall()
    {
        return true;
    }

    /**
    * Module tables installation
    * 
    * @return bool success
    **/
    protected function dbUninstall()
    {
        return true;
    }
}
