<IfModule mod_rewrite.c>
    #Options -MultiViews

    RewriteEngine On
    RewriteBase /tests/prestamod/web/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index_dev.php [QSA,L]
</IfModule>
php_flag display_errors on
